{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],

      "outputs": ["dist/**"]
    },
    "frontend#build": {
      "dependsOn": ["^build"],
      "env": ["OPENAI_API_KEY"],
      "outputs": [".next/**", "!.next/cache/**"]
    }
  }
}
